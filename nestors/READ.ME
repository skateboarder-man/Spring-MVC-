실행 방법 
1. 서버 셋팅
	1-1. Tomcat V9.0 서버 등록
	1-2. VM arguments (-Dspring.profiles.active=local) 입력
		(server -> Overview -> Open launch configuration -> Arguments -> VM arguments)
	1-3. Path 변경 "/"
	    (server -> Modules -> Edit -> Path 칸에 입력 "/")

2. Maven 업데이트

